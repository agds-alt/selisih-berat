# ==================== ENVIRONMENT CONFIGURATION ====================
# Selisih Berat - J&T Express
# Weight Entry Application - Production Ready Configuration
# Copy this file to .env.local and fill in your actual values

# ==================== SERVER CONFIGURATION ====================
# Environment: development | production | test
NODE_ENV=production

# Server port (default: 3000)
PORT=3000

# ==================== SUPABASE CONFIGURATION ====================
# Get these from your Supabase project settings
# REQUIRED for the application to work
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ==================== CLOUDINARY CONFIGURATION ====================
# Image upload and storage service
# Get these from your Cloudinary dashboard
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloud_name
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=your_upload_preset
NEXT_PUBLIC_CLOUDINARY_FOLDER=selisih-berat

# ==================== JWT & SECURITY ====================
# CRITICAL: Generate strong random strings (minimum 32 characters)
# Use: openssl rand -base64 32
# Or: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"

# Session secret for cookie encryption
SESSION_SECRET=your_super_secret_session_key_min_32_characters_here

# JWT signing secrets
JWT_SECRET=your_super_secret_jwt_key_min_32_characters_here
JWT_REFRESH_SECRET=your_super_secret_refresh_jwt_key_min_32_characters_here

# CORS - Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# ==================== RATE LIMITING ====================
# Protect against brute force and DoS attacks

# Login rate limiting (default: 5 attempts per 15 minutes)
LOGIN_RATE_LIMIT_WINDOW=900000
LOGIN_RATE_LIMIT_MAX=5

# API rate limiting (default: 30 requests per minute)
API_RATE_LIMIT_WINDOW=60000
API_RATE_LIMIT_MAX=30

# ==================== ANALYTICS ====================
# Optional: Enable analytics tracking

# Enable/disable analytics
NEXT_PUBLIC_ANALYTICS_ENABLED=false

# Google Analytics ID (optional)
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Custom analytics endpoint (optional)
NEXT_PUBLIC_ANALYTICS_ENDPOINT=https://your-analytics-api.com/track

# ==================== FEATURE FLAGS ====================
# Enable/disable specific features

# PWA (Progressive Web App) support
NEXT_PUBLIC_ENABLE_PWA=true

# Offline mode support
NEXT_PUBLIC_ENABLE_OFFLINE=true

# Photo upload feature
NEXT_PUBLIC_ENABLE_PHOTO_UPLOAD=true

# Barcode scanner feature
NEXT_PUBLIC_ENABLE_BARCODE_SCANNER=true

# ==================== APP CONFIGURATION ====================
# Application metadata

NEXT_PUBLIC_APP_NAME=Selisih Berat
NEXT_PUBLIC_APP_VERSION=1.0.0
NEXT_PUBLIC_BASE_URL=https://yourdomain.com

# ==================== NOTES ====================
# 1. Never commit .env.local to version control
# 2. Always use strong, unique secrets in production
# 3. Rotate secrets periodically for better security
# 4. Use different secrets for development and production
# 5. Keep this .env.example file updated with new variables
